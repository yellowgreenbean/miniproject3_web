# 오늘의 하루

## 0. 기본 정보
- 이름 : 오늘의 하루 (김연두)
- 배포 주소 : https://miniproject3-web.vercel.app
- 사용한 AI 모델/API : Google Gemini API — `gemini-3-flash-preview`

## 1. 한 줄 소개
시험 준비생이 "오늘 뭘 얼마나 해야 하는지" 매번 직접 계산하지 않도록, AI 공부 선배가 대화로 목표를 듣고 일기·달성률까지 참고해 날짜별 학습량을 짜주는 할 일 관리 서비스입니다.

## 2. 이런 사람을 위해 만들었습니다
- 대상 : 시험이나 자격증을 준비하지만 계획 짜는 걸 혼자 해내야 하는 사람

- 그 사람이 지금 겪는 불편 :
  - 총 분량과 마감일은 정해져 있어도, 그걸 "오늘 몇 페이지"로 나누는 계산은 매번 스스로 해야 한다.
  - 컨디션이 안 좋아 하루 이틀 밀리기 시작하면, 애초에 짜둔 계획이 현실과 안 맞아 통째로 포기하게 된다.
  - 기존 할 일 앱은 완료/미완료만 보여줄 뿐, 왜 못 했는지는 아무도 묻지 않는다.

- 이 서비스를 쓰면 달라지는 것 :
  - 채팅창에 목표와 마감일만 말해도 날짜별 분량이 나오고, 버튼 하나로 오늘 할 일에 들어간다.
  - 최근 일기에 힘들다는 말이 있으면 선배가 먼저 그걸 알아채고 이번 주 분량을 줄일지 물어본다.
  - 최근 일주일 달성률이 낮으면 다그치지 않고 이유부터 물은 뒤, 그에 맞춰 계획을 다시 짜준다.

## 3. 주요 기능 (3개 이내)

**1) 오늘의 할 일** — 오늘 날짜의 to-do를 우선순위 카드로 보여줍니다. 쉼표·줄바꿈으로 여러 할 일을 한 번에 입력할 수 있고, 카드를 드래그해 순서를 바꾸거나 체크로 완료 처리할 수 있습니다.

**2) 달력 · 일기 · 친구 공유** — 월 달력에서 원하는 날짜를 골라 할 일과 일기를 확인/작성합니다. 이메일로 친구를 추가하면 승인 절차 없이 그 친구의 달력을 읽기 전용으로 볼 수 있고(일방향), 일기는 친구에게도 노출되지 않습니다.

**3) 공부 선배 AI 챗봇** — 대화로 목표(시험/마감일/부담 영역/하루 가능 시간)를 수집하고, 최근 일기와 달성률을 근거로 코칭한 뒤 날짜별 학습 계획을 제시합니다. 계획 옆 "to-do 추가" 버튼을 누르면 1)번 화면의 할 일로 바로 등록됩니다.

## 4. AI를 어디에 어떻게 썼나요

- **AI가 하는 일** :
  - 첫 방문이면 선배가 먼저 인사를 건네고, 목표 → 마감일 → 부담 과목 → 하루 가능 시간을 한꺼번에 묻지 않고 한두 개씩 대화로 물어봅니다.
  - 매 턴마다 그 사용자의 최근 일기·달성률·현재 목표를 함께 보고 답을 만들기 때문에, "그냥 챗봇"이 아니라 그 사람 상황에 맞춰 말을 겁니다. 일기에 피로·아픔 같은 말이 있으면 안부부터 묻고 분량을 줄일지 제안하고, 달성률이 80% 이상이면 칭찬을, 50% 미만이면 원인을 먼저 묻습니다.
  - 목표 정보가 충분히 모이면 날짜별 분량과 학습 방법을 하나의 대화 안에서 함께 제시합니다.

- **입력으로 무엇을 주고, 무엇을 받나요** : 전용 백엔드 서버 없이 Vercel Route Handler(`app/api/study-coach/route.js`)가 Supabase를 직접 조회해 컨텍스트를 만들고, 그 안에서 바로 Gemini를 호출합니다.

  | 시스템이 주입하는 값 | 어디서 가져오나 |
  | --- | --- |
  | `today` | 서버 기준 오늘 날짜(KST) |
  | `recent_diary` | 최근 3일 `diaries` |
  | `completion_rate` | 최근 7일 `todos` 완료 비율 |
  | `current_goal` | `goals` 테이블의 현재 목표 + D-Day |
  | 이전 대화 | `study_chat_logs` 최근 12턴 |

  Gemini에는 `responseSchema`로 아래 JSON 형태를 강제해서 받습니다.
  - `reply` — 사용자에게 보여줄 대화 한 마디
  - `goalUpdate` — 이번 턴에 새로 확정된 목표 값 (없으면 null)
  - `planItems` — `[{date, amount, method}]` 형태의 날짜별 계획 (제시할 때만 채워짐)

- **AI가 이상한 답을 줬을 때 어떻게 처리했나요** :
  - **형식이 깨질 때** — `responseMimeType: application/json` + `responseSchema`로 JSON만 나오게 강제하고, 그래도 파싱이 안 되거나 `reply`가 없으면 예외를 던져 500을 내려보냅니다.
  - **화면이 깨지지 않게** — 실패 응답은 클라이언트에서 별도의 "에러 말풍선"으로만 표시되고, 나머지 대화는 그대로 유지됩니다. 빈 메시지는 애초에 400으로 막아 API 호출까지 가지 않습니다.
  - **날짜를 마음대로 지어낼 때** — `goalUpdate.deadline`은 `YYYY-MM-DD` 정규식을 통과할 때만 저장하고, 아니면 원래 값을 그대로 둡니다.
  - **계획이 일부만 채워졌을 때** — `date`/`amount`/`method` 중 하나라도 비어 있는 항목은 화면에 내보내기 전에 걸러냅니다.

- **프롬프트에서 신경 쓴 점 1가지** : 처음에는 "필요한 정보를 한 번에 다 물어봐"라고만 지시했더니, 답변이 설문지처럼 길어져 대답할 마음이 안 생겼습니다. 그래서 "한 번에 너무 많은 정보를 주지 말고, 답변은 항상 사용자가 쉽게 답할 수 있는 질문 하나로 끝낼 것"을 별도 제약으로 못박았습니다. 이 한 줄 덕분에 목표 수집이 폼이 아니라 진짜 선배와 대화하는 흐름으로 바뀌었고, 이게 이 서비스에서 AI가 존재하는 이유이기도 합니다.

## 5. 데이터 설계

- **테이블 이름과 역할** :

  | 테이블 | 역할 |
  | --- | --- |
  | `profiles` | 이메일로 다른 사용자를 찾기 위한 공개 프로필 (가입 시 자동 생성) |
  | `friends` | 내가 이메일로 추가한 친구 목록 (승인 없는 일방향 공유) |
  | `todos` | 날짜별 할 일 |
  | `diaries` | 날짜별 일기 |
  | `goals` | 공부 선배가 대화로 수집한 현재 목표 |
  | `study_chat_logs` | 공부 선배와의 대화 기록 (멀티턴 문맥 유지용) |

- **주요 컬럼** :
  - `todos` : id, user_id, date, content, priority, is_done, created_at
  - `diaries` : id, user_id, date, content, created_at
  - `goals` : user_id(PK), subject, deadline, hard_area, daily_time, created_at, updated_at
  - `study_chat_logs` : id, user_id, role(user/assistant), content, created_at
  - `friends` : id, owner_id, friend_id, created_at

- **이렇게 설계한 이유** :
  - `goals`는 사용자당 목표를 하나만 유지하면 되므로 `user_id`를 그대로 기본 키로 잡았습니다. 대화 중 새 정보가 나올 때마다 `upsert(onConflict: user_id)`로 덮어쓰기만 하면 되어 별도 조회/분기 없이 갱신이 끝납니다. 한 번에 다 안 물어보고 하나씩 채우는 구조라 네 컬럼 모두 nullable로 뒀습니다.
  - `study_chat_logs`는 "질문/답변" 한 쌍이 아니라 `role`/`content`로 한 행 = 한 턴이 되도록 설계했습니다. 이렇게 하면 저장된 행을 그대로 Gemini의 `contents` 배열로 매핑할 수 있어 변환 로직이 없어집니다. 정렬은 `created_at`이 아니라 `id`로 하는데, 사용자 턴과 선배 턴을 한 번의 insert로 같이 넣다 보니 `created_at`이 같아져 순서가 뒤집힐 수 있기 때문입니다.
  - RLS는 테이블 성격에 따라 다르게 걸었습니다. `todos`는 "본인 것 또는 내가 추가한 친구 것"까지 조회를 허용하지만, `diaries`·`goals`·`study_chat_logs`는 친구에게도 보여선 안 되는 개인 기록이라 `auth.uid() = user_id`로 본인만 접근하도록 막았습니다. 특히 일기 내용이 그대로 AI 프롬프트에 들어가기 때문에, 이 테이블들이 새면 안 된다고 판단했습니다.

## 6. 화면 흐름
1. 첫 화면에서 서비스 소개를 보고 이메일로 **회원가입 또는 로그인**합니다.
2. 로그인하면 바로 **화면 1(오늘)**로 들어와 오늘 할 일을 확인하고, 입력창에 할 일을 적어 카드로 추가합니다. 체크로 완료 표시하거나 드래그로 순서를 바꿉니다.
3. 하단 탭에서 **화면 2(달력)**로 이동해 다른 날짜를 선택하고, 그날의 할 일과 일기를 씁니다. 이메일로 친구를 추가하면 상단 pill 버튼으로 친구 달력을 읽기 전용 열람할 수 있습니다.
4. 하단 탭에서 **화면 3(공부 선배)**으로 이동하면 선배가 먼저 인사하며 어떤 시험/목표를 준비 중인지 물어봅니다.
5. 목표 → 마감일 → 부담 과목 → 하루 가능 시간 순으로 몇 마디 대화를 주고받으면, 선배가 최근 일기와 달성률을 언급한 뒤 날짜별 학습 계획을 제시합니다.
6. 원하는 날짜의 **"to-do 추가"** 버튼을 누르면 그 계획이 실제 할 일로 등록되고, 화면 1·2에서 바로 확인됩니다.
7. 다음에 다시 들어와도 목표와 대화 기록이 남아 있어, 선배가 이전 맥락을 기억한 채로 대화를 이어갑니다.

## 7. 기획서와 달라진 점
- 뺀 것 :
  1. 자연어 입력을 AI가 우선순위별로 자동 정렬해주는 기능. 지금은 입력한 순서대로 우선순위가 매겨지고, 필요하면 사용자가 드래그로 직접 바꿉니다.
  2. 기한/전체 분량/하루 가능 분량/하루 시간을 한 번에 입력받는 폼 방식의 공부 계획 화면. 대화형 챗봇으로 완전히 바꿨습니다.

- 뺀 이유 :
  1. AI 기능을 두 개로 쪼개면 둘 다 얕아질 것 같았습니다. 요구사항이 "동작하는 AI 기능 1개 이상"이라, 개수를 늘리기보다 공부 선배 하나를 제대로 만드는 쪽을 택했습니다. 우선순위 정렬은 AI 없이 수동으로도 충분히 대체되지만, 상황에 맞는 학습량 재배분은 대체가 어렵다고 봤습니다.
  2. 폼은 네 칸을 다 채워야만 결과를 받을 수 있는데, 정작 이 서비스가 필요한 사람은 "전체 분량이 정확히 몇 페이지인지"조차 정리 못 한 경우가 많습니다. 빈칸이 있어도 대화가 이어지는 챗봇 쪽이 실제 상황에 더 맞았고, 일기·달성률을 근거로 먼저 말을 거는 것도 고정 폼으로는 애초에 불가능했습니다.

- 시간이 더 있었다면 넣었을 것 :
  - 달성률이 떨어졌을 때 사용자가 채팅방에 들어오지 않아도 선배가 먼저 말을 거는 기능. 지금은 사용자가 화면 3에 들어와야 코칭이 시작됩니다.
  - 제시된 `planItems`를 한 번에 전부 to-do로 등록하는 버튼. 지금은 날짜마다 하나씩 눌러야 합니다.
  - 달성률에 따라 실제로 목장에 양 아이템이 늘어나는 화면. 지금은 프롬프트 안에서 보상을 "언급"만 하고, 실제 시각적 보상과는 아직 연결돼 있지 않습니다.

## 8. 만들면서 가장 오래 막혔던 것
- 문제 : 공부 선배와 대화는 정상적으로 되는데, **아무것도 저장되지 않는** 문제가 있었습니다. 새로고침하면 대화가 통째로 사라지고, 방금 말한 목표를 다음 턴에 선배가 다시 물어봤습니다. 화면에는 에러가 하나도 안 뜨고 답변도 매끄럽게 와서, 정상 동작처럼 보이는 게 오히려 원인을 더 찾기 어렵게 만들었습니다.

- 어떻게 해결했나 : 원인이 한 겹이 아니라 여러 겹이어서 하나씩 벗겨냈습니다.
  1. Supabase에 `goals`/`study_chat_logs` 테이블 자체가 없었습니다. 코드는 이 테이블들을 조회하고 있었는데, 조회 결과에서 `data`만 꺼내 쓰고 `error`는 확인하지 않고 있었던 탓에 테이블이 없어도 그냥 빈 값으로 흘러가 답변만 정상적으로 나왔던 겁니다. `supabase/study-coach.sql`로 두 테이블과 RLS 정책을 만들어 해결했습니다.
  2. `goals` 갱신에 충돌 기준을 지정하지 않아 `upsert`가 새 행만 계속 만들 뻔했습니다. `user_id`를 기본 키로 잡고 `onConflict: "user_id"`를 명시했습니다.
  3. 사용자 메시지를 AI 호출 *전에* 저장하고 있어서, 호출이 실패하면 답변 없는 사용자 턴만 기록에 남아 다음 대화의 맥락이 어긋났습니다. 성공했을 때만 사용자·선배 두 턴을 함께 저장하도록 순서를 바꿨습니다.
  4. 두 턴을 한 번에 insert하다 보니 `created_at`이 같은 값이 되어 순서가 뒤집힐 수 있었습니다. 정렬 기준을 `id`로 바꿔 해결했습니다.

  가장 크게 배운 점은 **`error`를 확인하지 않으면 실패가 조용히 성공처럼 보인다**는 것이었습니다. `data`만 보고 넘어갔던 습관 때문에, 사실은 테이블 부재 하나였을 문제를 여러 개의 증상으로 착각하고 한참 헤맸습니다.
