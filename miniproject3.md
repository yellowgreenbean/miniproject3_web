# 미니프로젝트 3 :: 웹사이트 구현 계획서

> 본 계획서는 [`프로젝트3 파일.md`](./프로젝트3%20파일.md)의 요구사항(REQ-01~05, 기술 스택, 제출 양식)을 기준으로 작성되었습니다.

## 0. 서비스 개요

- **서비스 이름(가안)** : 오늘의 하루 (Today & Diary)
- **한 줄 소개** : 그날의 할 일을 우선순위와 함께 관리하고, 하루를 일기로 기록하며, AI 공부 선배에게 공부 방법을 물어볼 수 있는 개인 생산성 웹 서비스
- **기술 스택 (고정, REQ 기준)** : Next.js / Vercel(배포) / Supabase(DB+인증) / Gemini API(AI)

## 1. 이런 사람을 위해 만든다

- **대상** : 하루 할 일을 관리하고 싶은데, 무엇부터 해야 할지 정리가 안 되고, 하루를 기록하는 습관도 만들고 싶은 사람
- **겪는 불편** :
  - 할 일을 적어도 우선순위 없이 나열만 하게 됨
  - 공부할 내용은 많은데 어떤 방식으로 공부해야 할지 막막함
  - 하루를 기록하는 일기 습관이 잘 지속되지 않음
- **달라지는 점** :
  - 할 일을 입력만 하면 AI가 우선순위를 자동으로 정리해줌
  - "공부 선배" 챗봇에게 공부 내용을 말하면 공부 방법을 추천받을 수 있음
  - 로그인만 하면 그날의 할 일이 첫 화면에 바로 보여서 진입 장벽이 낮음

## 2. 화면 구성 (토글 3화면 + 인증)

로그인/회원가입은 서비스 진입 전 별도 흐름이며, 로그인 후에는 하단 탭(홈/달력/일기 아이콘)으로 **화면 1 / 화면 2 / 화면 3**을 토글하여 이동합니다.

### 인증 (화면 진입 전)
- 이메일 회원가입 / 로그인 (Supabase Auth)

### 화면 1 :: 오늘의 할 일 (로그인 후 첫 화면)
- 오늘 날짜의 to-do list 표시
- 자연어로 할 일 입력(쉼표·줄바꿈으로 여러 개 한번에) → 카드형으로 항목별 등록
- 완료 체크, 카드를 잡고 끌어서(드래그 앤 드롭) 우선순위 수동 조정

### 화면 2 :: 달력형 할 일 + 일기 + 친구 공유
- 월 전체 달력 UI(6주 그리드, 이전·다음 달 이동, 오늘/선택 날짜 표시, 할 일 있는 날짜에 점 표시)에서 날짜 선택 → 해당 날짜의 to-do list 조회/설정
- **일기 작성** : 선택한 날짜의 할 일 목록 바로 아래에 그날의 일기를 쓰고 저장하는 입력란 제공. 이미 쓴 내용은 자동으로 불러와 수정 가능, 내용을 비우고 저장하면 삭제됨
- **친구 공유(이메일 기반, 승인 절차 없음)**
  - 이메일을 입력해 친구를 추가하면 승인 없이 즉시 그 친구의 달력을 열람 가능
  - **일방향 공유** : 내가 상대를 추가해야 내가 볼 수 있음. 서로 보려면 양쪽 다 각자 상대를 추가해야 함
  - 상단에 "내 달력 / 친구 이메일" 전환 pill 버튼으로 전환, 친구는 추가·삭제 가능
  - 친구 달력은 **읽기 전용** — 체크 표시·우선순위 변경·할 일 추가·일기 열람 모두 불가 (일기는 애초에 본인 것만 조회되도록 설계, 헤더에 "OO님의 달력 (읽기 전용)" 표시)
  - 가입되지 않은 이메일이나 본인 이메일을 입력하면 에러 메시지로 안내 (REQ-05, 화면 안 깨짐)

### 화면 3 :: 공부 선배 챗봇
- 사용자가 **할 일 기한 / 전체 분량 / 하루 가능 분량 / 하루 투자 가능 시간**을 입력하면, AI가 날짜별 공부 분량과 공부 방법을 계산해 대화형으로 제시
- 제시된 날짜별 계획을 **버튼 클릭으로 to-do list에 바로 추가** 가능 (화면 1·2의 `todos`에 반영)

## 3. AI 기능 설계 (REQ-02)

이 서비스는 **AI 기능 2가지**를 포함합니다.

### 3-1. 할 일 우선순위 자동 추천
- **입력** : 사용자가 자연어로 적은 할 일 텍스트(또는 여러 줄)
- **처리** : Gemini API에 할 일 목록을 전달해 우선순위(중요도/순서)를 함께 반환받음
- **출력** : 우선순위가 매겨진 to-do 항목으로 화면에 반영
- **예외 처리** : AI 응답이 비정상(형식 오류, 빈 값)이면 우선순위 없이 원문 그대로 저장하고 사용자가 직접 순서를 조정할 수 있게 함

### 3-2. 공부 선배 챗봇 (공부 계획 자동 분배)
- **입력** : 사용자가 아래 항목을 채팅창(폼)에 입력
  - 할 일 기한 (마감일, 예: 며칠 후 / 특정 날짜)
  - 할 일 전체 분량 (예: 총 페이지 수, 문제 수, 챕터 수 등)
  - 하루에 할 수 있는 분량 (사용자가 하루 최대로 처리 가능한 분량)
  - 하루에 투자할 수 있는 시간
- **처리** :
  1. Gemini API에 위 4가지 값을 프롬프트로 전달
  2. AI가 "기한까지 남은 일수"와 "하루 가능 분량/시간"을 고려해 하루치 공부량과 학습 방법(어떤 순서로, 어떻게 공부하면 좋을지)을 날짜별로 계산
  3. 사용자가 입력한 하루 가능 분량/시간을 초과하지 않도록 분배 (초과할 경우 기한 내 완료가 어렵다는 점과 대안을 함께 안내)
- **출력** :
  - 날짜별 학습 분량 + 공부 방법을 대화형으로 제시 (예: "8/10 : 1~20p, 요약 위주로 학습")
  - 각 날짜 항목 옆에 **"to-do list에 추가"** 버튼 제공 → 클릭 시 해당 날짜의 `todos`에 자동 등록
- **예외 처리** :
  - 필수 입력값(기한/분량/하루 가능 분량/시간) 중 하나라도 비어 있으면 AI 호출 전에 입력 안내 표시
  - API 호출 실패 시 "잠시 후 다시 시도해주세요" 등 안내 문구 표시, 화면이 깨지지 않도록 처리
  - 기한 대비 분량이 지나치게 많아 물리적으로 불가능한 경우, 그대로 쪼개서 보여주고 "기한 내 완료가 어려울 수 있음"을 함께 안내

## 4. 데이터 설계 (REQ-03, Supabase)

| 테이블 | 역할 | 주요 컬럼 |
| --- | --- | --- |
| `users` | Supabase Auth 사용자 정보(내장) | id, email |
| `profiles` | 이메일로 다른 사용자를 찾기 위한 공개 프로필 | id(=auth.users.id), email |
| `friends` | 내가 이메일로 추가한 친구 목록 (승인 절차 없음) | id, owner_id, friend_id, created_at |
| `todos` | 날짜별 할 일 항목 | id, user_id, date, content, priority, is_done, created_at |
| `diaries` | 날짜별 일기 | id, user_id, date, content, created_at |
| `study_chat_logs` (선택) | 공부 선배 챗봇 대화 기록 | id, user_id, question, answer, created_at |

- 사용자별 데이터 구분이 필요하므로(친구 공유 기능 포함) Supabase 인증을 사용
- `profiles`는 회원가입 시 트리거로 자동 생성되며, 이메일 검색을 위해 조회는 전체 공개
- `todos`의 조회 정책(RLS)은 "본인 것 또는 `friends`에 내가 추가한 사람 것"만 허용 (전체 공개 아님), 수정/삭제는 본인 소유 데이터만 가능
- `diaries`는 친구에게도 공개하지 않는 개인 기록이므로, 조회·작성·수정·삭제 모두 본인 소유 데이터만 가능하도록 RLS 설계. 화면 2(달력)에서 날짜별로 작성

## 5. 화면 흐름 (사용자 시나리오)

1. 이메일로 회원가입 또는 로그인
2. 로그인 성공 시 화면 1(오늘의 할 일)로 진입, 오늘 날짜의 to-do list 확인
3. 자연어로 할 일 입력 → AI가 우선순위를 매겨 목록 반영
4. 화면 2(달력)로 이동해 다른 날짜의 할 일을 설정하거나, 그날의 일기를 쓰거나, 이메일로 친구를 추가해 그 친구의 달력을 읽기 전용으로 열람
5. 화면 3(공부 선배 챗봇)으로 이동해 "공부 선배"에게 기한/분량/하루 가능 분량·시간을 입력
6. AI가 계산한 날짜별 공부 계획을 확인하고, 원하는 항목을 버튼으로 to-do list에 추가

## 6. 프로젝트3 요구사항 대응표

| 요구사항 | 대응 내용 |
| --- | --- |
| REQ-01 (주제 자유) | 기획서(하루 할일/달력/일기/AI) 기반, 아래 "고려사항"에서 범위 조정 여부 결정 |
| REQ-02 (AI 기능 1개 이상) | 우선순위 자동 추천 + 공부 선배 챗봇(기한/분량 기반 학습 계획 자동 분배) 2개 구현 |
| REQ-03 (Supabase 저장) | `todos`, `diaries` 테이블에 저장, 새로고침/재접속 후에도 유지 |
| REQ-04 (Vercel 배포) | 배포 후 시크릿 창/다른 기기에서 접속 테스트 필수 |
| REQ-05 (최소 사용성) | 첫 화면에 오늘의 할 일 노출로 목적 직관적 전달, AI 실패/빈 입력 시 예외 처리 |

## 7. 고려사항 (범위 관련)

- 요구 기능이 (로그인/회원가입 + 달력형 to-do + 우선순위 + 친구 공유 + 일기장 + AI 2종) 로 다소 많은 편입니다.
- "먼저 작게 완성"이라는 프로젝트 원칙에 따라, **다른 사용자 to-do 열람 기능**은 처음 검토했던 "전체 공개 토글" 대신 **이메일 기반 친구 추가(승인 없음, 일방향, 읽기 전용)** 방식으로 단순화해서 구현했습니다. `profiles`/`friends` 테이블과 RLS 정책 추가만으로 구현 가능해 범위를 크게 늘리지 않으면서 REQ-01의 "서로의 to-do list를 볼 수 있음" 요건을 충족했습니다.

## 8. 다음 작업 순서 (제안)

1. Supabase 프로젝트 생성 및 `todos` / `diaries` / `profiles` / `friends` 테이블 + 인증 설정
2. Next.js 프로젝트 생성, 로그인/회원가입 화면 구현
3. 화면 1(오늘의 할 일) + 드래그 우선순위 조정 구현
4. 화면 2(달력형 to-do + 일기 작성 + 이메일 기반 친구 공유) 구현
5. 화면 3(공부 선배 챗봇) 구현
6. Vercel 배포 및 다른 기기/시크릿 창 테스트
7. 서비스 설명서(`미니프로젝트3_이름.md`) 작성 및 제출 준비

## 9. 계획서 이후 추가 구현한 기능

위 1~8은 구현 전에 작성한 계획입니다. 아래는 계획에 없었지만 만들면서 추가한 기능들입니다.

### 9-1. 구글 캘린더 연동 (읽기 전용)

- **왜 넣었나** : 할 일은 이 앱에 있는데 수업·약속 같은 일정은 구글 캘린더에 있어서, 하루를 보려면 두 군데를 봐야 했습니다. 달력 화면에서 둘을 같이 보게 만들었습니다.
- **연동 흐름** : 구글 로그인(Supabase OAuth) → 콜백에서 세션에 딱 한 번 실려 오는 구글 토큰을 저장 → 달력 화면이 열릴 때마다 그 달의 일정을 조회
  - Supabase는 로그인 순간에만 구글 토큰을 넘겨줄 뿐 보관·갱신해주지 않기 때문에, `google_tokens` 테이블에 직접 보관하고 만료 1분 전에 앱이 스스로 갱신합니다.
  - `access_type=offline` + `prompt=consent`로 동의를 받아야 refresh token이 내려옵니다. 이게 없으면 로그인 후 1시간만 일정이 보입니다.
- **요청 범위** : `calendar.readonly` — 읽기만 합니다. 쓰기까지 하려면 `calendar.events` 범위가 필요하고 구글 심사 조건이 달라집니다.
- **화면 표시**
  - 월 달력에서 구글 일정이 있는 날짜에 **파란 점** 표시 (내 할 일은 초록 점)
  - 선택한 날짜의 일정을 **할 일 카드와 같은 카드**로 표시 — 2.5px 윤곽선, 20px 라운드, 6px 오프셋 그림자, 42px 원형 아이콘 배지
  - 단, 색은 초록이 아니라 **구글 파랑**을 씁니다. 형태를 통일하되 색으로 "내 할 일 / 구글 일정"을 구분하기 위해서입니다.
  - 일정 제목은 18px / 굵기 800으로, 할 일 카드 본문(16px)보다 한 단계 크게 표시
- **친구 달력에는 표시하지 않음** : 친구의 구글 토큰이 없기도 하지만, 있더라도 남의 일정을 보여주면 안 되기 때문입니다.
- **연동 해제** : 달력 화면의 "연동 해제" 버튼으로 저장된 토큰을 삭제합니다.

### 9-2. 챗봇 옵션 — 밀린 할 일 정리 모드

화면 3의 챗봇을 **두 가지 모드**로 나누고, 새 모드를 추가했습니다.

| 모드 | 역할 |
| --- | --- |
| 공부 선배 | (기존) 목표를 대화로 모아 날짜별 학습 계획을 제시 |
| 밀린 할 일 | (추가) 못 끝낸 할 일을 모아 다시 나눠 배치 |

- **기간 설정** : 3·7·14·30일 버튼 또는 직접 입력(1~90일). 며칠 전부터 거슬러 올라가 모을지 사용자가 정합니다.
  - 범위는 **어제까지**로 끊습니다. 오늘 할 일은 아직 밀린 것이 아니기 때문입니다.
  - 고른 기간이 실제로 며칠인지(예: `2026. 8. 3. ~ 2026. 8. 9.`) 화면에 같이 보여줍니다.
- **처리** : 그 기간의 완료되지 않은 할 일을 모아 화면에 **그대로 목록으로 보여주고**, 같은 목록을 AI에게 넘겨 앞으로 며칠에 어떻게 나눠 할지 제안받습니다.
  - 목록을 화면에 먼저 보여주는 이유는, AI가 요약하면서 항목을 빠뜨리거나 지어내도 사용자가 원본과 대조할 수 있게 하기 위해서입니다.
  - 오래 밀려서 지금 해도 의미가 없는 항목은 "이건 넘기자"고 덜어내도록 프롬프트에 지시했습니다.
- **출력** : 날짜별 분량 + 방법을 카드로 제시하고, **개별 추가** 또는 **한 번에 모두 추가** 버튼으로 `todos`에 등록 → 화면 1·2(달력)에 바로 반영
- **대화 기록 분리** : 두 모드의 대화는 `study_chat_logs.mode`로 나눠 저장합니다. 한 스레드에 섞으면 "밀린 일 정리하자"와 "목표가 뭐야?"가 번갈아 나와 문맥이 무너지기 때문입니다.
- **예외 처리**
  - 해당 기간에 밀린 할 일이 **하나도 없으면 AI를 호출하지 않고** 안내만 표시합니다. 없는 목록을 두고 계획을 지어내는 것을 막고 호출도 아낍니다.
  - AI가 만든 날짜가 `YYYY-MM-DD` 형식이 아니거나 **이미 지나간 날짜면 버립니다.** 지나간 날로 재배치된 계획은 달력에 넣어도 의미가 없습니다.
  - **원본 할 일은 지우거나 옮기지 않습니다.** 새 날짜에 새 항목이 추가될 뿐이고, 예전 기록은 그대로 남습니다.

### 9-3. 추가된 테이블

4장의 표에 더해 아래 테이블을 사용합니다.

| 테이블 | 역할 | 주요 컬럼 |
| --- | --- | --- |
| `google_tokens` | 구글 캘린더 조회용 토큰 보관 | user_id(PK), access_token, refresh_token, expires_at, updated_at |
| `goals` | 공부 선배가 대화로 수집한 현재 목표 | user_id(PK), subject, deadline, hard_area, daily_time |
| `study_chat_logs` | 챗봇 대화 기록 (모드별로 분리) | id, user_id, role, content, mode, created_at |

- 세 테이블 모두 친구에게도 보여선 안 되는 개인 기록이라, RLS를 `auth.uid() = user_id`로 걸어 **본인만** 접근할 수 있게 했습니다. 특히 일기 내용과 목표가 그대로 AI 프롬프트에 들어가기 때문에 이 테이블들이 새면 안 된다고 판단했습니다.
- `goals`와 `google_tokens`는 사용자당 한 행만 있으면 되므로 `user_id`를 그대로 기본 키로 두고 `upsert`로 갱신합니다.

## 10. 한계점

### 10-1. 구글 캘린더 연동

- **읽기 전용이라 한 방향으로만 흐릅니다.** 구글 일정은 앱에서 볼 수 있지만, 앱에서 만든 할 일은 구글 캘린더에 올라가지 않습니다. 챗봇의 "달력에 넣기"도 앱 내부 `todos`에만 저장됩니다. 화면에 둘이 나란히 보이기 때문에 사용자가 구글에도 등록될 거라 기대하기 쉬운 지점입니다.
- **구글로 로그인한 사용자만 쓸 수 있습니다.** 토큰은 구글 로그인 콜백에서만 저장되므로, 이메일로 가입한 사용자는 캘린더만 따로 붙일 수 없습니다. 또 한 번 "연동 해제"하면 달력 화면에 다시 연동하는 버튼이 없어서, 로그아웃 후 구글로 재로그인해야 복구됩니다.
- **실패해도 사용자에게 이유를 알려주지 못합니다.** 토큰 없음·갱신 실패·구글 API 오류가 모두 같은 결과(섹션이 화면에서 사라짐)로 처리됩니다. 어제까지 보이던 일정이 오늘 사라져도 왜 그런지, 어떻게 고치는지 안내가 없습니다.
- **테스트 모드에서는 7일마다 재로그인이 필요합니다.** 구글 OAuth 게시 상태가 "테스트 중"이면 refresh token이 7일 후 만료됩니다. 프로덕션으로 전환하려면 민감 범위(`calendar.readonly`)에 대한 구글 검토를 받아야 합니다.
- **기본 캘린더 하나만 읽습니다.** 구독 중인 학사일정·공휴일·팀 캘린더는 보이지 않고, 캘린더를 고르는 화면도 없습니다.
- **여러 날에 걸친 일정이 첫날에만 표시됩니다.** 일정의 시작일만 사용하고 종료일을 읽지 않아, 3일짜리 여행 일정은 첫날에만 나타납니다.
- **한 번에 250개까지만 가져옵니다.** 페이지 처리를 하지 않아 6주 범위에 일정이 그보다 많으면 조용히 잘립니다.
- **달력을 열 때마다 매번 새로 조회합니다.** 캐시가 없어 날짜만 바꿔 눌러도 6주치를 다시 받아옵니다. 구글 응답이 느리면 달력 화면 전체가 느려지고, 실패해도 재시도하지 않습니다.
- **실시간 반영이 아닙니다.** 구글에서 일정을 바꿔도 앱을 다시 열어야 반영됩니다.
- **토큰이 암호화 없이 저장됩니다.** RLS로 본인만 접근하도록 막았지만, DB가 유출되면 남의 캘린더를 읽을 수 있는 자격 증명이 그대로 노출됩니다.
- **시간대가 KST로 고정**되어 있어, 다른 시간대에서 쓰면 날짜가 어긋납니다.

### 10-2. 밀린 할 일 정리 모드

- **원본 할 일이 그대로 남습니다.** 사용자 데이터를 말없이 지우지 않으려는 선택이지만, 그 결과 다음에 또 "밀린 할 일"로 다시 잡힙니다. 반복해서 계획을 만들면 비슷한 항목이 여러 날짜에 쌓일 수 있습니다.
- **이미 잡혀 있는 일정과의 충돌을 보지 않습니다.** 재배치할 날짜를 고를 때 그날 이미 있는 할 일이나 구글 일정을 확인하지 않아서, 원래도 바쁜 날에 분량을 더 얹을 수 있습니다.
- **한 번에 60개까지만 AI에게 넘깁니다.** 그보다 많이 밀려 있으면 뒤쪽 항목은 제안에 반영되지 않습니다.
- **두 모드가 서로의 대화를 모릅니다.** 문맥이 섞이지 않게 일부러 나눴지만, 그 대가로 공부 선배는 사용자가 무엇을 밀렸는지 모르고 밀린 할 일 모드는 목표 대화를 참고하지 못합니다.
- **추가한 계획을 되돌릴 방법이 화면에 없습니다.** 잘못 눌러 등록하면 달력에서 직접 지워야 합니다.
- **하루 분량 조절의 근거가 약할 수 있습니다.** "하루 투자 가능 시간"을 `goals`에서 가져오는데, 공부 선배 모드에서 목표를 등록한 적이 없으면 "미정"으로 들어갑니다.

### 10-3. 공통

- **AI 응답의 품질 자체는 보장할 수 없습니다.** 형식은 `responseSchema`로 강제하고 날짜·필수값은 코드에서 걸러내지만, 제안한 분량이 실제로 적절한지까지는 검증하지 못합니다.
- **알림이 없습니다.** 사용자가 앱에 들어와야 코칭이 시작되고, 밀린 할 일이 쌓여도 먼저 알려주지 않습니다.
