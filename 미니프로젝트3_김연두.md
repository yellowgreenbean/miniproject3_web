# 오늘의 하루

## 0. 기본 정보
- 이름 : 오늘의 하루 (김연두)
- 배포 주소 : https://miniproject3-web.vercel.app
- 사용한 AI 모델/API : Google Gemini API — `gemini-3-flash-preview` 

## 1. 한 줄 소개
시험을 앞두고 "뭘 얼마나 해야 할지" 막막한 사람에게, AI 공부 선배가 대화로 목표를 묻고 일기·달성률을 읽어 날짜별 공부량을 나눠주는 할 일 관리 서비스입니다.

## 2. 이런 사람을 위해 만들었습니다
- 대상 : 시험이나 자격증을 준비하면서 혼자 계획을 세워야 하는 사람

- 그 사람이 지금 겪는 불편 :
  - 총 분량은 아는데 "오늘 몇 페이지"로 쪼개는 걸 매번 직접 계산해야 한다.
  - 계획을 한 번 세워두면 컨디션이 나빠도 그대로 남아, 못 지킨 날이 쌓이면 계획 자체를 포기하게 된다.
  - 할 일 앱은 못 지킨 걸 그대로 보여줄 뿐, 왜 못 했는지는 묻지 않는다.

- 이 서비스를 쓰면 달라지는 것 :
  - 마감일과 하루 가능 시간만 대화로 말하면 날짜별 분량이 나오고, 버튼 한 번으로 그날 할 일에 들어간다.
  - 일기에 "몸살 났다"고 써두면 선배가 먼저 안부를 묻고 이번 주 분량을 줄일지 제안한다.
  - 달성률이 낮으면 혼내지 않고 원인(난이도·시간 부족·컨디션)을 물어본 뒤 다시 배분해준다.

## 3. 주요 기능 (3개 이내)

**1) 오늘의 할 일** — 날짜별 to-do를 우선순위 순서로 관리합니다. 쉼표나 줄바꿈으로 여러 개를 한 번에 입력하고, 체크·순서 변경이 가능하며 주간 날짜 이동을 지원합니다.

**2) 달력 + 일기 + 친구 공유** — 월 달력에서 날짜를 골라 그날의 할 일과 일기를 쓰고, 이메일로 친구를 추가해 친구 달력을 읽기 전용으로 볼 수 있습니다. 승인 절차 없는 일방향 공유이고, 일기는 친구에게도 공개되지 않습니다.

**3) 공부 선배 AI 챗봇** — 대화로 목표를 수집하고, 일기와 달성률을 근거로 코칭하며, 날짜별 학습 계획을 제시합니다. 각 날짜 옆 "to-do 추가" 버튼을 누르면 1)의 할 일로 바로 등록됩니다.

## 4. AI를 어디에 어떻게 썼나요

- **AI가 하는 일** : 세 가지를 한 번의 호출에서 동시에 처리합니다.
  1. **대화형 목표 수집** — 시험/목표 → 마감일 → 가장 부담스러운 영역 → 하루 투자 가능 시간을 한 번에 몰아 묻지 않고 순서대로 한두 개씩 질문합니다.
  2. **맥락 기반 감성 코칭** — 최근 일기에 피로·질병·슬픔이 보이면 안부를 묻고 분량을 10~20% 줄일지 제안합니다.
  3. **분량 재배분** — 달성률 80% 이상이면 칭찬과 목장 테마 보상을 언급하고, 50% 미만이면 원인을 물은 뒤 계획을 다시 나눕니다.

- **입력으로 무엇을 주고, 무엇을 받나요** :

  Route Handler(`app/api/study-coach/route.js`)가 Supabase에서 아래를 직접 조회해 시스템 프롬프트에 주입합니다.

  | 주입 데이터 | 내용 |
  | --- | --- |
  | `today` | 오늘 날짜(KST) |
  | `recent_diary` | 최근 3일 일기 |
  | `completion_rate` | 최근 7일 할 일 달성률(%) |
  | `current_goal` | 등록된 목표 + 마감일 D-Day |
  | 대화 기록 | 최근 12턴 |

  받는 것은 아래 JSON 한 덩어리입니다. Gemini의 `responseSchema`로 형식을 강제했습니다.

  - `reply` : 사용자에게 보여줄 대화 메시지
  - `goalUpdate` : 이번 턴에 확정된 목표 값 (있을 때만)
  - `planItems` : `[{date, amount, method}]` 날짜별 계획 (제시하는 턴에만)

- **AI가 이상한 답을 줬을 때 어떻게 처리했나요** : 실패 지점마다 다르게 막았습니다.
  - **형식이 깨지는 경우** — `responseMimeType: application/json` + `responseSchema`로 애초에 JSON만 나오게 하고, 그래도 파싱이 실패하거나 `reply`가 비면 예외를 던져 500으로 내려보냅니다.
  - **화면이 깨지는 경우** — 클라이언트가 실패 응답을 `error` 역할의 말풍선으로 그려서 "잠시 후 다시 시도해주세요"만 뜨고 대화창은 그대로 유지됩니다. 빈 메시지는 400으로 막습니다.
  - **날짜를 지어내는 경우** — `goalUpdate.deadline`이 `YYYY-MM-DD` 정규식을 통과할 때만 저장하고, 아니면 기존 마감일을 유지합니다.
  - **계획 항목이 반쯤 빈 경우** — `date`/`amount`/`method` 중 하나라도 없는 항목은 걸러내고 보여줍니다.
  - **호출이 실패한 턴** — 대화 기록에 저장하지 않습니다. 사용자 메시지만 남으면 다음 턴의 문맥이 어긋나기 때문에, 성공했을 때만 사용자·선배 두 턴을 함께 기록합니다.

- **프롬프트에서 신경 쓴 점 1가지** : **답변을 항상 질문 하나로 끝내게 한 것.** 처음엔 필요한 4가지를 한 번에 물어봤는데, 답장이 설문지처럼 길어져서 대답할 마음이 사라졌습니다. "한 번에 너무 많은 정보를 주지 말 것, 마지막은 사용자가 쉽게 대답할 수 있는 질문 하나로 끝낼 것"을 제약으로 넣으니 대화가 실제 선배와 얘기하는 흐름이 됐습니다. 목표 수집이 폼이 아니라 대화로 성립하는 게 이 서비스의 핵심이라, 이 한 줄이 기능 자체를 지탱합니다.

## 5. 데이터 설계

- **테이블 이름과 역할** :

  | 테이블 | 역할 |
  | --- | --- |
  | `profiles` | 이메일로 사용자를 찾기 위한 공개 프로필 (가입 시 자동 생성) |
  | `friends` | 내가 추가한 친구 목록 (승인 없는 일방향) |
  | `todos` | 날짜별 할 일 |
  | `diaries` | 날짜별 일기 |
  | `goals` | 챗봇이 대화로 수집한 현재 목표 |
  | `study_chat_logs` | 공부 선배 대화 기록 |

- **주요 컬럼** :
  - `todos` : id, user_id, date, content, priority, is_done, created_at
  - `diaries` : id, user_id, date, content, created_at
  - `goals` : **user_id(PK)**, subject, deadline, hard_area, daily_time, updated_at
  - `study_chat_logs` : id, user_id, role, content, created_at
  - `friends` : id, owner_id, friend_id, created_at

- **이렇게 설계한 이유** :
  - **`goals`는 user_id를 기본 키로 잡았습니다.** 목표는 사용자당 하나만 유지하면 되고, 대화 중 새 정보가 나올 때마다 `upsert`로 덮어써야 하기 때문입니다. 컬럼 4개(subject / deadline / hard_area / daily_time)는 온보딩 질문 순서와 1:1로 맞췄습니다. 한 번에 다 안 물어보고 하나씩 채워가는 구조라, 나머지가 비어 있어도 저장되도록 전부 nullable로 뒀습니다.
  - **`study_chat_logs`는 한 행 = 한 턴(role/content) 구조입니다.** 기획서에는 question/answer 한 쌍으로 적었는데, 그러면 Gemini에 보낼 때 매번 풀어야 하고 턴 수가 안 맞을 때 깨집니다. role/content로 두면 `contents` 배열에 그대로 매핑됩니다. 정렬은 `created_at` 대신 `id`로 합니다. 두 턴을 한 번의 insert로 넣으면 `created_at`이 같은 값이 되어 순서가 뒤집힐 수 있기 때문입니다.
  - **RLS는 테이블 성격에 따라 다르게 걸었습니다.** `todos`는 "본인 것 또는 내가 추가한 친구 것"까지 조회를 허용하지만, `diaries`·`goals`·`study_chat_logs`는 친구에게도 보이면 안 되는 개인 기록이라 `auth.uid() = user_id`로 본인만 접근하게 막았습니다. 일기가 AI 프롬프트에 들어가는 만큼 여기가 새면 안 된다고 봤습니다.

## 6. 화면 흐름

1. 첫 화면에서 서비스 소개를 보고 **회원가입 / 로그인** 중 선택해 이메일로 가입합니다.
2. 로그인하면 **화면 1 (오늘)** 로 들어와 오늘 날짜의 할 일을 확인합니다. 입력창에 할 일을 적어 추가하고, 체크와 순서 변경을 합니다.
3. 하단 탭에서 **화면 2 (달력)** 로 이동해 다른 날짜의 할 일을 관리하고, 그날의 일기를 씁니다. 이메일로 친구를 추가하면 상단 pill 버튼으로 친구 달력을 읽기 전용으로 볼 수 있습니다.
4. 하단 탭에서 **화면 3 (공부 선배)** 로 이동하면 선배가 먼저 인사하며 준비 중인 시험을 묻습니다.
5. 대화로 목표 → 마감일 → 부담스러운 영역 → 하루 가능 시간을 답하면, 선배가 **일기와 달성률을 먼저 언급한 뒤** 날짜별 공부 계획을 제시합니다.
6. 마음에 드는 날짜의 **"to-do 추가"** 버튼을 누르면 그 날짜의 할 일로 등록되고, 화면 1·2에서 바로 확인됩니다.
7. 다음에 다시 들어와도 대화 기록과 목표가 남아 있어, 선배가 이전 맥락을 기억한 채로 이어서 대화합니다.

## 7. 기획서와 달라진 점

- **뺀 것** :
  1. **AI 할 일 우선순위 자동 추천** (기획서 3-1). 지금은 입력한 순서대로 우선순위가 매겨지고, 사용자가 직접 순서를 바꿉니다.
  2. **폼 방식의 공부 계획 입력.** 기한·전체 분량·하루 가능 분량·하루 시간 4칸을 채워 제출하는 형태로 기획했는데, 대화형 챗봇으로 바꿨습니다.

- **뺀 이유** :
  1. AI 기능을 2개로 나누면 둘 다 어설퍼질 것 같았습니다. 요구사항이 "실제로 동작하는 AI 기능 1개 이상"이라, 개수를 늘리는 대신 챗봇 하나를 제대로 만드는 쪽을 택했습니다. 우선순위 추천은 AI가 없어도 수동 정렬로 대체가 되지만, 계획 배분은 대체가 안 됩니다.
  2. 폼은 4칸을 다 채우기 전엔 아무것도 못 얻습니다. 정작 이 서비스가 필요한 사람은 "전체 분량이 몇 페이지인지"조차 정리가 안 된 상태인 경우가 많아서, 빈칸을 남겨둔 채로도 대화가 진행되는 챗봇이 맞다고 판단했습니다. 결과적으로 일기·달성률을 근거로 말을 거는 것도 폼에서는 불가능했습니다.

- **시간이 더 있었다면 넣었을 것** :
  - 계획을 세운 뒤 실제 달성률이 떨어지면 **선배가 먼저 말을 거는 것.** 지금은 사용자가 채팅방에 들어와야만 코칭이 시작됩니다.
  - `planItems` 전체를 **한 번에 to-do로 추가**하는 버튼. 지금은 날짜별로 하나씩 눌러야 합니다.
  - 달성률에 따라 실제로 **양 아이템이 늘어나는 목장 화면.** 프롬프트에서 보상으로 언급만 하고 있어서, 말과 화면이 아직 연결돼 있지 않습니다.

## 8. 만들면서 가장 오래 막혔던 것

- **문제** : 챗봇이 **대화는 되는데 아무것도 저장되지 않았습니다.** 새로고침하면 대화가 사라지고, 목표를 말해도 다음 턴에 선배가 다시 물어봤습니다. 화면에는 에러가 안 뜨고 답변도 멀쩡히 와서, 원인을 찾는 데 가장 오래 걸렸습니다.

- **어떻게 해결했나** : 원인이 하나가 아니라 여러 겹이었고, 하나씩 벗겨냈습니다.
  1. **테이블이 없었습니다.** 코드는 `goals`와 `study_chat_logs`를 조회하는데 Supabase에 만들어두질 않았습니다. REST로 직접 찔러보고 `PGRST205 (Could not find the table)`를 확인했습니다. 조회 결과를 `data`만 꺼내 쓰고 `error`는 안 보고 있어서, 테이블이 없어도 그냥 빈 값으로 흘러가 답변은 정상적으로 나왔던 것이 화면에 아무 표시가 없던 이유였습니다. SQL 파일(`supabase/study-coach.sql`)로 두 테이블과 RLS를 만들어 해결했습니다.
  2. **`upsert`에 충돌 기준이 없었습니다.** `goals`에 user_id 기준을 안 알려줘서 목표 갱신이 동작하지 않았습니다. user_id를 기본 키로 잡고 `onConflict: "user_id"`를 명시했습니다.
  3. **실패한 턴이 기록에 남았습니다.** 사용자 메시지를 AI 호출 *전에* 저장하고 있어서, 호출이 실패하면 답변 없는 사용자 턴만 남고 다음 대화의 문맥이 어긋났습니다. 성공한 뒤에 두 턴을 함께 저장하도록 순서를 바꿨습니다.
  4. **정렬 기준이 불안정했습니다.** 두 턴을 한 번에 넣으면 `created_at`이 동일해져 사용자·선배 순서가 뒤집힐 수 있어서, `id` 기준 정렬로 바꿨습니다.

  가장 크게 배운 건 **에러를 삼키면 디버깅이 몇 배로 어려워진다**는 점이었습니다. `data`만 보고 `error`를 무시한 코드가 "조용히 실패"를 만들어서, 실제로는 1번 문제 하나였을 것을 한참 헤맸습니다.
