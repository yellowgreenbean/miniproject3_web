# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# python venv (unrelated to this project)
.venv

# design reference images (not ours to redistribute)
/image

# 제출용 사본과 압축본. 소스 전체를 그대로 복사한 것이라 저장소에 다시 넣지 않는다.
# (같은 이름의 보고서 .md 는 추적 대상이므로 여기에 넣지 않을 것)
/미니프로젝트3_김연두/
/미니프로젝트3_김연두.zip

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code local settings
/.claude/
